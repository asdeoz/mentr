overwrite: true
schema:
  - https://graphql.us.fauna.com/graphql:
      headers:
        Authorization: "Bearer ${FAUNA_ADMIN_SECRET}"
documents: "./src/**/*.graphql"
generates:
  ./generated/sdk.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-graphql-request"